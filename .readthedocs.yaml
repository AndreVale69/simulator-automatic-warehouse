version: 2

formats: all

build:
  os: ubuntu-lts-latest
  jobs:
    pre_build:
      - export PYTHONPATH=..:.
  tools:
    python: "3.12"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
